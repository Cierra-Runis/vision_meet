import 'package:vision_meet/index.dart';

class LinkQQTile extends StatelessWidget {
  const LinkQQTile({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return BasedListTile(
      leadingIcon: FontAwesomeIcons.qq,
      titleText: '微信',
      detailText: '***',
      onTap: () {},
    );
  }
}
