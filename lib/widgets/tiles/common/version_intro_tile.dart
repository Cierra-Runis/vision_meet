import 'package:vision_meet/index.dart';

class VersionIntroTile extends StatelessWidget {
  const VersionIntroTile({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return BasedListTile(
      leadingIcon: Icons.fiber_new_rounded,
      titleText: '版本介绍',
      onTap: () {},
    );
  }
}
