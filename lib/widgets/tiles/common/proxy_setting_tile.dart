import 'package:vision_meet/index.dart';

class ProxySettingTile extends StatelessWidget {
  const ProxySettingTile({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return BasedListTile(
      leadingIcon: FontAwesomeIcons.p,
      titleText: '代理设置',
      onTap: () {},
    );
  }
}
