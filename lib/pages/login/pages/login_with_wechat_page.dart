import 'package:vision_meet/index.dart';

class LoginWithWeChatPage extends StatelessWidget {
  const LoginWithWeChatPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('使用微信登录'),
      ),
    );
  }
}
