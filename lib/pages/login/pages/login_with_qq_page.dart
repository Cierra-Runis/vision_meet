import 'package:vision_meet/index.dart';

class LoginWithQQPage extends StatelessWidget {
  const LoginWithQQPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('使用 QQ 登录'),
      ),
      // TODO:
      body: const Placeholder(),
    );
  }
}
